"use client";

/**
 * Next.js client boundary for @agent-office/pixel-icons WeaponIcon.
 * The package component is framework-agnostic; this thin re-export adds the
 * "use client" directive App Router needs.
 */
export { WeaponIcon } from "@agent-office/pixel-icons/react";
export type { WeaponIconProps } from "@agent-office/pixel-icons/react";
